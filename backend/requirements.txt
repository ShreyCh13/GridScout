# Node Ranking Engine Dependencies

# Core dependencies (required)
pandas>=1.5.0
numpy>=1.21.0

# API server dependencies (optional - only needed for REST API)
flask>=2.0.0
flask-cors>=3.0.0
gunicorn>=21.0.0

# Testing dependencies (optional)
pytest>=7.0.0
requests>=2.28.0

